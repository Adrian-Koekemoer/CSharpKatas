namespace Exercises.Gcd;

public static class GcdMath
{
    // Return the greatest common divisor of a and b.
    // Rules:
    // - gcd(x, 0) = |x|, gcd(0, y) = |y| (When one of the numbers is Zero return the non-zero number)
    // - gcd(0, 0) => throw ArgumentException
    // - negative inputs allowed; treat via absolute values
    public static int Gcd(int a, int b)
    {
        // TODO: Implement (Euclidean algorithm recommended)
        throw new NotImplementedException();
    }
}
